# Vagrant + Docker
```powershell
vagrant box add bento/ubuntu-20.04 --provider virtualbox --force

vagrant box list
vagrant validate
vagrant up --provider virtualbox
vagrant status
vagrant ssh
vagrant halt
vagrant destroy -f
```