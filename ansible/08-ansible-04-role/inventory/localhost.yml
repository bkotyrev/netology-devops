---
all:
  vars:
    ansible_connection: local
    ansible_python_interpreter: "{{ ansible_playbook_python }}"
    ansible_become: false
  children:
    clickhouse:
      hosts:
        localhost:
    vector:
      hosts:
        localhost:
    lighthouse:
      hosts:
        localhost:
