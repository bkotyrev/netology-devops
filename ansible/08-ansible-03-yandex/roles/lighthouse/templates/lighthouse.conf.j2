server {
    listen {{ lighthouse_nginx_listen_port }} default_server;
    listen [::]:{{ lighthouse_nginx_listen_port }} default_server;
    server_name {{ lighthouse_nginx_server_name }};

    root {{ lighthouse_document_root }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
